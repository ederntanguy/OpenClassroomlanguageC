#ifndef mainh // Si la constante n'a pas été définie le fichier n'a jamais été inclus
#define mainh // On définit la constante pour que la prochaine fois le fichier ne soit plus inclus
typedef struct Coordonnees Coordonnees;
struct Coordonnees
{
    int x;
    int y;
};
#endif